import itertools

def fact(n):
    mul = 1
    for i in range(1, n + 1):
        mul = i * mul
    return mul


def primes():
    i = 1
    
    while True:
        i += 1
        if ((fact(i - 1) + 1) % i) == 0:
            yield i
